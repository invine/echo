# name: Build and Push Docker Image

# on:
#   push:
#     branches:
#       - main

# jobs:
#   build-and-push-image:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v3
#       - uses: aws-actions/configure-aws-credentials@v1
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: us-east-1
#       - run: docker login -u AWS -p ${{ secrets.AWS_ECR_PUBLIC_REGISTRY_PASSWORD }} public.ecr.aws
#       - run: docker build -t public.ecr.aws/"${{ secrets.AWS_ECR_PUBLIC_REGISTRY_ALIAS }}"/"${{ secrets.ECR_REPO_NAME }}":"${{ github.sha }}" -t public.ecr.aws/"${{ secrets.AWS_ECR_PUBLIC_REGISTRY_ALIAS }}"/"${{ secrets.ECR_REPO_NAME }}":latest .
#       - run: docker push public.ecr.aws/"${{ secrets.AWS_ECR_PUBLIC_REGISTRY_ALIAS }}"/"${{ secrets.ECR_REPO_NAME }}":"${{ github.sha }}"
#       - run: docker push public.ecr.aws/"${{ secrets.AWS_ECR_PUBLIC_REGISTRY_ALIAS }}"/"${{ secrets.ECR_REPO_NAME }}":latest

name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v4
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - run: echo "🍏 This job's status is ${{ job.status }}."
